# ═══════════════════════════════════════════════════════════════
# requirements_rpi.txt — Raspberry Pi (ARM64 / aarch64)
# Tested on: RPi 4 / RPi 5  |  OS: Raspberry Pi OS 64-bit (Bookworm)
#
# STEP 1 — System packages (run in terminal FIRST):
#   sudo apt update && sudo apt install -y \
#       python3-pip python3-opencv \
#       libopencv-dev libcamera-dev libcamera-apps \
#       libgstreamer1.0-dev gstreamer1.0-plugins-base \
#       gstreamer1.0-plugins-good gstreamer1.0-libav
#
# STEP 2 — Enable Pi Camera:
#   sudo raspi-config → Interface Options → Camera → Enable
#
# STEP 3 — Install Python packages:
#   pip install -r requirements_rpi.txt
# ═══════════════════════════════════════════════════════════════

# ── Core ────────────────────────────────────────────────────────
ultralytics>=8.3.0

# ── Computer Vision ─────────────────────────────────────────────
# Use system OpenCV (installed via apt) for GStreamer support.
# If you must pip-install, use headless build:
opencv-python-headless>=4.9.0

# ── Numerical ───────────────────────────────────────────────────
numpy>=1.24.0

# ── PyTorch for ARM64 ───────────────────────────────────────────
# Official ARM wheel — CPU only (no CUDA on RPi)
torch>=2.2.0
torchvision>=0.17.0
# If the above fails on your RPi, install the ARM-specific wheel:
#   pip install https://download.pytorch.org/whl/cpu/torch-2.2.0-cp311-cp311-linux_aarch64.whl

# ── Utilities ───────────────────────────────────────────────────
PyYAML>=6.0
Pillow>=10.0.0
tqdm>=4.65.0
requests>=2.31.0
psutil>=5.9.0
scipy>=1.11.0
